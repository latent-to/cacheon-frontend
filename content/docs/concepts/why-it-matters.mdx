---
title: Why inference optimization matters
description: Training keeps advancing, but for most real-world use cases, the bottleneck is inference and deployment. Latency, throughput, and cost decide whether a model is usable in production, and existing benchmarks remain fragmented.
---

## Training advances. Serving decides usability.

Better models only matter if you can serve them. Frontier quality keeps improving, yet for most deployments the limiting factors are not research headlines but **inference**: latency, throughput, and cost on the hardware and traffic you actually run.

- **Latency**: how fast responses start and stream (time-to-first-token and tokens per second). Users and agents feel every delay.
- **Throughput**: how many concurrent requests you can serve on fixed hardware.
- **Cost**: GPU utilization and serving efficiency. Dollars per million tokens.

These are **inference-time** constraints. Cacheon treats inference optimization as a competitive problem and pushes the network to discover the fastest, most efficient serving stacks in the open.

## The optimization surface

Modern inference has dozens of knobs, and each one interacts with the others:

| Layer          | Techniques                                                  | Impact                             |
| -------------- | ----------------------------------------------------------- | ---------------------------------- |
| **Memory**     | KV cache quantization, eviction, paging, offloading         | Max context length, max batch size |
| **Compute**    | FlashAttention, custom CUDA kernels, fused operations       | Tokens/sec, prefill speed          |
| **Scheduling** | Continuous batching, chunked prefill, preemptive scheduling | Throughput under load              |
| **Decoding**   | Speculative decoding, Medusa heads, lookahead               | Output tokens/sec                  |
| **Runtime**    | Rust/C++ servers, kernel-level optimizations, memory layout | End-to-end latency                 |

Existing solutions like vLLM, TensorRT-LLM, and SGLang each make different tradeoffs across these axes. No single team has tried every combination. Cacheon creates an incentive to search this space exhaustively.

## The benchmark problem

There is no shared, verifiable benchmark for full-stack inference performance:

- vLLM publishes its own benchmarks against its own baselines
- Academic papers measure individual techniques in isolation <sup><a href='#references'>[1]</a></sup> <sup><a href='#references'>[2]</a></sup>
- Inference providers report numbers against undisclosed configurations

The **numbers don't compare.** Teams shipping production inference have no way to know which combination of techniques actually wins for their specific model and workload.

<Callout title="What Cacheon does">
  Fix the model, fix the hardware, fix the prompts, fix the scoring, and let anyone submit a full
  serving stack. The subnet produces an ordered ranking that is apples-to-apples comparable.
</Callout>

## What winning looks like

The endpoint is not a paper or a blog post. The endpoint is an inference server ranked #1 on the leaderboard, serving a top open-source model faster than vLLM on identical hardware. That is the product: the fastest way to run that model, discovered through open competition.

Product-market fit comes when the system evolves from a benchmark leader into the backend people actually choose for latency-sensitive inference. That includes agent frameworks that need long-context throughput, Bittensor subnets that depend on reliable Qwen/GLM serving, and aggregators looking for differentiated performance. The target users are teams willing to pay for lower TTFT, higher tokens-per-second, and better GPU efficiency without building their own inference stack.

See the [roadmap](/docs/roadmap) for how V1 through V4 scale from single-model arena to production inference marketplace.

## References

[1]: Xavier Lyu - https://x.com/xavi3rlu/status/2049950829097127951

[2]: Jianyang Gao - https://x.com/gaoj0017/status/2037532673812443214
